# 路线图

里程碑 + 任务清单。任务用 checkbox 跟踪：`[ ]` 待办 / `[~]` 进行中 / `[x]` 完成。
进度日志见 [进度.md](进度.md)。

## M0 立项与地基调研 ✅ 完成
- [x] 确定核心定位（个人非商业 · 更丰富使用自己的 Claude Code）
- [x] 确定数据源 stock-sdk
- [x] 确定 AI 接入方式（无头 claude -p + MCP）
- [x] 确定市场范围 / 包管理 / 语言
- [x] 定名「韭见」并重命名项目目录
- [x] 初始化文档体系（README/CHANGELOG/决策/路线图/进度/调研）
- [x] 地基调研：核实承重假设 D9（裁决：个人非商业用 🟢 GREEN）
- [x] 据调研结论拍板 D8（electron-vite + React/TS + shadcn + KLineChart + electron-builder）
- [x] 拍板 D10（先自用·预留分享·仅订阅 CLI 可插拔鉴权）
- [x] 确认 M0 出口裁决 🟢

## M1 骨架最小闭环 ✅ 完成（含真机核心验证）
> 目标：Electron 启动 → 渲染层触发 → main 起 `claude -p` 挂 stock-sdk MCP → 流式回传 → 渲染。
- [x] pnpm + electron-vite + TS 工程初始化（typecheck + build 全绿）
- [x] main / preload / renderer 三层 + contextBridge IPC 契约（shared 单一事实源 + Zod 校验 + sender 校验）
- [x] claude 二进制探测 + 登录态检测（处理 alias/PATH/横幅噪音；实测解析到 claude.sh）
- [x] claude orchestrator 服务（spawn、stream-json readline 流式、AbortController 取消、SIGKILL 兜底、错误码分类）
- [x] stock-sdk MCP 内联挂载（--strict-mcp-config）+ 工具允许列表
- [x] K线图表直拉（main 用 stock-sdk，实测 sh600519 拿到 5929 根日K）
- [x] 最小 UI：状态横幅 + 市场/代码输入 → 触发 → 流式分析面板 + 成本遥测
- [x] 端到端跑通一次真实分析（无头实测：claude.sh + MCP `get_a_share_quotes` + 真实 stream-json，结果正确「茅台 1309.60 −1.24%」，成本 $0.238）
- [x] MCP allowedTools 核对：~70 工具确走 ToolSearch（已含）；stream-json 解析器与真实 schema 零偏差，无需改

## M2 单股深度分析（首个真功能）🟡 进行中
- [x] 实时行情头（stock-sdk 直拉 quote，红涨绿跌；FUND 用净值 nav）
- [x] K线图表渲染 + 指标（MA 叠加主图 / VOL / MACD 副图）
- [x] 分析 prompt 模板（趋势/指标/资金/风险）
- [x] 图文分析报告 markdown 渲染（react-markdown + 暗色 .prose-jj 样式）
- [ ] 资金流/龙虎榜等维度面板（当前由 claude 经 MCP 覆盖，未做独立面板）
- [x] FUND 涨跌幅不伪造（语义存疑 → 置 null，只展示净值 + 原始 change）

## M3 多市场 + 自选股 🟡 进行中
- [x] 港股/美股/基金ETF 数据路径（quote + kline 已分市场路由）
- [x] 自选股列表 + 本地持久化（localStorage，侧栏增删 + 点选加载）
- [ ] 列表行情 + 迷你图（每行实时价/涨跌 + sparkline）

## M4 复盘 / 每日摘要 ✅ 核心完成
- [x] 一键盘后复盘：全自选股紧凑快照(价/涨跌/MA多空/MACD/RSI/KDJ) + 大盘(上证) → 一次 claude 出整体复盘（情绪/分化/值得关注/风险）
- [x] 真机 e2e 验证（3股+大盘，~$0.11，报告含真实指标/情绪/分化）
- [x] 定时收盘自动触发（app 内调度：工作日到点自动复盘 + 系统通知，⚙ 设置可配开关/时间；注：需 app 保持开启）
- [x] 复盘/分析历史留存（localStorage 最近 50 条；右侧抽屉：列表 + 查看 markdown + 删除/清空）
- [x] 后台常驻触发：菜单栏 tray（关窗缩 tray 不退出）+ 开机自启（setLoginItemSettings openAsHidden）+ tray「打开/立即复盘/退出」+ ⚙ 设置开关。定时复盘后台常驻生效（不再依赖手动开 app）。
  - 真机 boot 验证：含 tray 的 main 启动不崩、干净退出。tray 可见性/关窗缩 tray/开机自启 需真机交互确认。

## M5 打包分发 🟡 进行中
- [x] macOS 本地打包（electron-builder，无签名 `.app`，261M，已验证真启动）
- [x] 解决中文 productName rename 问题（mac.executableName=jiujian，显示名仍「韭见」）
- [ ] DMG 产出（`pnpm dist:mac`，配置就绪未跑）/ Windows NSIS
- [ ] 代码签名 + macOS 公证（分发给他人才需，个人用免）
- [ ] 自动更新（electron-updater）
- [x] 首次启动引导（claude 未找到/未登录时，AI 栏显示三步设置指引：安装文档 + `claude auth login` 可复制 + 重新检测；左侧图表照常）
- [x] app 图标（squircle 圆角 + 透明角 + 金色「韭」+ 烛台，`build/icon.png` 1024²）
- [x] 投资建议免责声明（底部常驻条）

> 排障备忘：首次打包卡在 `unpack-electron` = npmmirror 多线程下载把 electron zip 下坏（461M vs 正确 102M）。解决：`rm ~/Library/Caches/electron/electron-v*.zip` 后单线程 `curl` 重下。
