# 进度日志

按日期记录实际进展。最新在上。任务规划见 [路线图.md](路线图.md)。

## 2026-06-01

- M1 收尾：真机 `pnpm dev` boot 冒烟测通过（main+preload 编译、renderer dev server 5173、electron 起、无崩）。
- git 初始化并提交骨架（commit；已 gitignore 含 token 的继承 `.mcp.json` + `.specstory/` 对话日志，未入库）。
- **M2 增量**：实时行情头（stock-sdk 直拉 quote）+ K线指标（MA/VOL/MACD）。
- 验证：typecheck + build 全绿；quote smoke（茅台 1309.6 −1.24%、沪深300ETF nav 4.9147）。
- 待续 M2：分析报告 markdown 渲染、资金流面板、FUND 涨跌幅语义校正。

## 2026-05-29

- 项目立项。确定核心定位：BYO 订阅的 Electron AI 股票分析应用，复用用户自己的 Claude Code 订阅冗余算力。
- 锁定一系列决策：分发模式、AI 接入方式、数据源、市场范围、包管理(pnpm)、语言(TS)。详见 [决策记录](决策记录.md)。
- 定名 **韭见 JiuJian**；项目目录由 `untitled folder` 重命名为 `jiujian`。
- 建立文档体系：README、CHANGELOG、决策记录、路线图、进度、地基调研。
- 完成**地基调研编排** `w06rfvlku`（4 agents / ~160k tokens）。结论已回填 [地基调研](研究/地基调研.md)。
- 🚦 裁决 **YELLOW**：技术闭环本机验证可跑；但两条载重风险——
  - ⚠️ **ToS**：官方明禁第三方通过用户 Pro/Max 凭据路由请求，可无预警执行。
  - ⚠️ **配额（颠覆原卖点）**：**2026-06-15 起** `claude -p` 改从独立月度 Agent SDK 额度（Pro $20/Max5x $100/Max20x $200）按 API 价扣费、不滚存。"蹭免费冗余算力"实质失效，卖点需重述（见 [D2](决策记录.md)）。
- 拍板技术栈（[D8](决策记录.md)）：electron-vite + React18/TS + shadcn/ui + KLineChart + electron-builder。
- 两条关键事实已由主控亲自二次核实（官方 support + legal 页面逐字）。

- 明确定位：**个人非商业**，更丰富使用自己的 Claude Code → D9 裁决翻 🟢 GREEN（ToS 消解、配额变化反成契合点）。
- 拍板 [D10](决策记录.md)：先自用·预留分享·仅订阅 CLI（架构留可插拔鉴权接口）。**M0 完成** ✅。

- **完成 M1 骨架**（手搓 electron-vite 工程）：shared 契约 → 安全窗口/生命周期 → claude 探测+可插拔鉴权 provider → 编排器(spawn claude -p stream-json + MCP) → IPC/preload 桥 → renderer 最小 UI → KLineChart 面板 + stock-sdk 直拉。
- 图表数据源拍板：图表 main 直拉 stock-sdk，分析走 claude（见 [D4](决策记录.md)）。
- **验证**：`pnpm typecheck` 绿 ✓；`electron-vite build` 三目标打包成功 ✓；stock-sdk 实拉 sh600519 5929 根日K ✓；claude 探测链实测解析到 claude.sh（已处理 alias + .zshrc 横幅噪音）✓。

> 待办：① 真机 `pnpm dev` 端到端跑一次真实分析（需登录态，会消耗 Agent SDK 额度）；② 首跑核对 MCP 工具 allowedTools 命名空间。之后进 M2 单股深度分析。
